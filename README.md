# nvim

### Requirements
1. neovim (obviously)
2. packer.nvim

### Installation
Using git:
    
	git clone https://github.com/Nova-archx86/nvim

	cp -r nvim .config/

After coppying the configs and entering in neovim:
	
	:PackerSync


